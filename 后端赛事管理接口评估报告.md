# 后端赛事管理接口评估报告

## 一、 评估结论

**是否可支撑业务完全实现：否**

虽然当前后端接口已实现了 `AGENT.md` 及各 Phase 自检清单中的核心业务流程（创建、发布、报名、成绩、通知），但在应对真实的赛事管理复杂场景时，仍存在部分关键业务闭环缺失、性能与安全机制不完善的问题。必须经过一轮迭代修复阻塞项（P0）后方可上线。

**建议迭代版本号**：v1.1.0-RC

---

## 二、 接口覆盖度矩阵

| 自检清单/业务规则条目 | 相关接口 URI | 功能维度 | 覆盖状态 | 备注说明 |
| :--- | :--- | :--- | :--- | :--- |
| **赛事创建与配置** | `POST /api/event/EventList` <br> `PUT /api/event/{id}` | 赛事创建/修改 | 部分满足 | 路径命名不规范（`EventList`作为POST路径），建议重构为 `POST /api/event` |
| **赛事管理员分配** | `POST /api/admin/events/{id}/admins` | 权限分配 | 完全满足 | 已实现超管/校管分配赛事管理员功能 |
| **赛事状态流转与发布** | `PUT /api/event/{id}/status` | 状态变更 | 完全满足 | 配合 `EventStatusScheduler` 定时任务，满足自动/手动状态流转 |
| **赛事撤回与删除** | `POST /api/admin/events/{id}/withdraw` <br> `DELETE /api/event/{id}` | 撤回/删除 | 完全满足 | 撤回与删除均有前置校验逻辑（如仅DRAFT可删，未开始可撤回） |
| **运动员资格申请与审核** | `POST /api/athlete`<br>`POST /api/event-admin/{id}/athlete-applications/batch-approve` | 审核/资质 | 完全满足 | 支持单条/批量审核，及异步站内信通知 |
| **项目报名与取消** | `POST /api/registration/apply/{projectId}` <br> `DELETE /api/registration/{id}` | 报名 | 完全满足 | 已实现7项硬性校验与并发原子扣减名额 |
| **成绩录入与发布** | `POST /api/score/upsert` <br> `PUT /api/score/publish/{eventId}` | 成绩录入 | 完全满足 | 遵循已发布不可修改规则，支持 Upsert |
| **成绩批量导入导出** | `POST /api/score/import/{eventId}` <br> `GET /api/score/export/{eventId}` | 数据交互 | 完全满足 | 具备逐行校验、部分成功容错与Excel分Sheet导出能力 |
| **赛程编排** | - | 赛程管理 | **缺失** | 当前系统仅有起止时间，缺乏具体道次、分组（预赛/决赛）编排能力 |
| **成绩申诉与名次重算**| - | 异常补偿 | **缺失** | 成绩发布后若有异议，当前无申诉流转及重算接口 |

---

## 三、 缺失或不符合项清单（优化建议）

### P0 级别（阻塞上线，必须修复）
1. **接口路径与规范不符**
   - **缺陷**：`EventController` 中的新增赛事接口为 `POST /api/event/EventList`，且部分接口注解权限与切面混合使用（如 `@PreAuthorize` 与 `@RequireRole` 混用）。
   - **修复建议**：统一 RESTful 规范，修改为 `POST /api/event`。统一全局权限校验机制，全部使用自定义注解 `@RequireRole` / `@RequireEventAdmin`。
   - **估时**：2小时
2. **幂等性与防重放缺失（报名场景）**
   - **缺陷**：虽然报名使用了 `current_count < max_participants` 的原子操作防超卖，但在弱网环境下，用户快速连点可能导致前端发起多个重复请求，后端若仅依赖 DB 唯一索引可能导致多余的 DB 压力。
   - **修复建议**：在 `POST /api/registration/apply/{projectId}` 等高频写接口增加 Redis 分布式锁或 Token 幂等校验。
   - **估时**：4小时

### P1 级别（影响体验，建议本期修复）
1. **赛程编排与分组能力缺失**
   - **缺陷**：实际运动会中存在“预赛-复赛-决赛”的分组和道次排排需求，当前仅有 `event_item`，粒度过粗。
   - **修复建议**：新增 `event_match_schedule`（赛程表），支持项目下多轮次生成及检录单导出接口。
   - **估时**：2天
2. **成绩申诉与修正补偿流转缺失**
   - **缺陷**：Phase 3 规定“已发布的成绩不可修改”，但在实际场景中，成绩录入错误是高发事件，若无申诉和特权修正接口，业务将陷入死锁。
   - **修复建议**：增加“成绩申诉申请”接口（用户侧）及“成绩更正与重新发布”接口（超管/赛事管理员侧），并附加审计日志。
   - **估时**：1天

### P2 级别（可迭代，后续版本规划）
1. **报名截止后邮件通知机制**
   - **缺陷**：当前系统仅有站内信通知，若用户未登录系统，可能错过参赛时间。
   - **修复建议**：在 `EventStatusScheduler` 定时任务将状态置为 `ONGOING` 前，增加异步邮件提醒功能。
   - **估时**：4小时
2. **系统操作审计日志（Data Audit）**
   - **缺陷**：对赛事删除、成绩修改等高危操作，缺乏独立的审计日志表，仅依靠 Logback 文件日志难以追溯。
   - **修复建议**：引入 `@Log` 注解配合 AOP，将关键接口的出入参、操作人持久化至 `sys_audit_log` 表。
   - **估时**：1天

---

## 四、 多维度深度验证报告

### 1. 数据完整性
- **满足度**：高。
- **分析**：所有 Controller 入参均配置了 `@Valid`，核心实体包含 `created_at`、`updated_at`。外键关联（如 `event_id`）在 DTO 中有明确定义。缺乏部分扩展字段（如 JSON 格式的 `extra_info`），但在当前需求范畴内已足够。

### 2. 性能与容量
- **满足度**：中。
- **分析**：
  - 并发报名：采用了基于 DB 的原子 UPDATE 乐观锁机制，能有效防止超卖，应对单校级别的 QPS（通常 < 500）性能达标。
  - 批量导入：成绩导入通过 `EasyExcel` 监听器逐行解析并支持部分成功，内存控制良好。
  - 排名计算：直接在 DB 层面或内存中排序，暂未引入 Redis ZSet。在项目报名人数 < 1000 的场景下无性能瓶颈。

### 3. 安全与权限
- **满足度**：高。
- **分析**：基于 JWT 实现了最小权限模型。利用 `@RequireEventAdmin` 切面，实现了行级数据权限控制（确保赛事管理员只能操作自己绑定的赛事），跨越了简单的角色 RBAC，达到了 ABAC/行级控制的效果。密码全量采用 BCrypt。

### 4. 异常与补偿
- **满足度**：低。
- **分析**：正如 P1 缺陷所述，系统对正向流程支持极好，但对逆向补偿（成绩改判、赛程因雨延期批量修改时间等）缺乏对应的事务接口，强依赖 DBA 连库改数据。
