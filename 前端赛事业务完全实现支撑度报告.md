# 前端赛事业务完全实现支撑度报告

## 一、 评估结论

**是否可支撑业务完全实现：否**

当前前端工程在基础骨架和页面展示上已有一定完成度，但距离端到端的赛事管理主流程闭环仍有较大差距。最致命的问题在于路由设计与组件落地脱节（如“赛事创建”点击后跳转至建设中页面），部分核心业务操作缺乏统一的向导或连贯的交互设计，导致管理员无法在系统中顺利走通“创建赛事 -> 配置项目 -> 开启报名”的标准流程。

**建议迭代版本号**：v1.1.0-RC

---

## 二、 页面流程与状态机分析

根据标准赛事管理主流程（创建→发布→报名→开赛→成绩→颁奖→归档），前端页面支撑情况如下：

### 1. 创建与配置阶段（缺失严重）
- **状态机**：`DRAFT` (草稿)
- **页面现状**：
  - `EventManage.vue` 列表页包含草稿状态展示。
  - **断点**：点击“新建赛事”按钮跳转到 `/event-create`，但 `EventCreate.vue` 是一个 `el-empty` 的占位页面（“赛事创建页面正在建设中...”）。
  - **断点**：缺乏赛事与项目的强级联视图。当前 `ProjectManage.vue` 是一个全局平铺列表，缺乏针对特定赛事（`/admin/events/:id/items`）的内聚管理视图。

### 2. 发布与报名阶段（支撑良好）
- **状态机**：`OPEN` (报名中) -> `CLOSED` (报名结束)
- **页面现状**：
  - `EventManage.vue` 支持下拉菜单执行“发布赛事”、“撤回赛事”状态操作。
  - 用户侧 `event/index.vue` (赛事大厅) 及 `event/detail.vue` 能够根据状态控制报名按钮的亮起/灰显。
  - 运动员申请审核页面 (`AthleteAudit.vue`) 与报名记录管理页面 (`RegistrationAudit.vue`) 功能完备。

### 3. 开赛与成绩阶段（基本支撑）
- **状态机**：`ONGOING` (进行中) -> `FINISHED` (已结束)
- **页面现状**：
  - `EventManage.vue` 列表中支持“结束赛事”操作。
  - `ScoreManage.vue` 支持成绩查询与状态区分。
  - 支持弹窗调用后端的成绩批量导入导出功能。

### 4. 颁奖与归档阶段（缺失）
- **状态机**：无独立归档状态
- **页面现状**：
  - 前端无独立的数据归档或电子奖状/证书生成页面。

---

## 三、 组件能力矩阵映射

| 前端组件/页面 | 对应后端接口 | 支撑能力分析 | 是否达标 |
| :--- | :--- | :--- | :--- |
| **`EventManage.vue`** | `/api/event/page`<br>`/api/event/{id}/status` | 列表渲染、状态流转下拉操作、二次确认弹窗等基础能力完备。 | ✅ 达标 |
| **`EventCreate.vue`** | `/api/event/EventList` | **空壳页面**，未实现动态表单和多步骤向导。实际创建逻辑被隐藏在 `EventManage` 的废弃 `dialogVisible` 中。 | ❌ 缺失 |
| **`EventAdminManager`** | `/api/admin/events/{id}/admins` | 抽屉/弹窗组件，支持为赛事分配管理员列表。 | ✅ 达标 |
| **成绩导入导出组件** | `/api/score/import/{id}`<br>`/api/score/export/{id}` | 在 `EventManage` 中集成了 `el-upload`，支持单文件上传；导出会通过 Blob 转换为本地下载。 | ✅ 达标 |
| **赛程/分组编排组件** | 无 | 完全未实现拖拽排阵、树形对阵图等复杂交互组件。 | ❌ 缺失 |

---

## 四、 阻塞列表与修复建议 (P0/P1)

### P0 阻塞项（必须修复方可跑通流程）
1. **赛事创建与编辑流程断链**
   - **现象**：`EventManage.vue` 中点击“新建赛事”跳入空页面；但 `EventManage.vue` 自身又保留了 `dialogVisible` 的创建逻辑代码却未启用。
   - **建议**：重构 `EventCreate.vue` 和 `EventEdit.vue`。采用分步表单设计（Step 1: 赛事基础信息 -> Step 2: 比赛项目配置 -> Step 3: 管理员分配），完成后再变更状态为发布。
2. **路由结构与业务层级不符**
   - **现象**：自检清单要求实现 `/admin/events/:id/items` 等嵌套路由，但实际路由 `router/index.js` 为平级结构（`/project-manage`），导致管理员在管理特定赛事时，需要在所有项目里通过筛选寻找，极易误操作。
   - **建议**：按照 RESTful 资源层级重构前端路由嵌套，提供“赛事详情管理 Layout”，左侧菜单动态显示该赛事的【项目、报名、成绩、人员】。

### P1 体验项（建议本期优化）
1. **并发与防抖控制缺失**
   - **现象**：前端在提交报名 (`apply`) 或发布成绩时，按钮未做防抖（Debounce）或严格的 `loading` 状态锁定，网络延迟时易产生多次提交。
   - **建议**：全局封装自定义指令 `v-debounce`，并在所有涉及多表写操作的按钮上强制绑定 `loading` 属性。
2. **数据可视化组件不完善**
   - **现象**：`Dashboard` 虽然接入了 `/api/event/total` 等接口，但缺乏针对赛事的具体转化漏斗图（如：访问 -> 申请运动员 -> 报名 -> 完赛 的漏斗）。
   - **建议**：引入 ECharts，增加赛事级维度的数据钻取面板。
