# Phase 2 自检清单 · 赛事核心

> 全部为 [x] 才允许进入 Phase 3

---

## 📅 赛事管理

- [x] 创建赛事时间校验：regStart < regEnd < eventStart < eventEnd
- [x] 创建赛事 maxItemsPerAthlete 必填，范围 1-20
- [x] 赛事创建后状态为 DRAFT
- [x] 发布赛事前校验：至少有一个 event_item，否则 400
- [x] 发布赛事前校验：至少有一个赛事管理员，否则 400
- [x] 发布成功后异步向所有注册用户发站内通知
- [x] 撤回赛事：仅 OPEN 且当前时间 < regStartTime 时可撤回
- [x] 草稿赛事通过公开接口访问返回 404
- [x] 删除赛事：仅 DRAFT 状态可删，同时删除关联 event_item 和绑定关系

---

## ⏱️ 定时任务

- [x] EventStatusScheduler 每分钟执行一次
- [x] OPEN + 当前时间 > reg_end_time → 自动变 CLOSED
- [x] CLOSED + 当前时间 > event_start_time → 自动变 ONGOING
- [x] ONGOING + 当前时间 > event_end_time → 自动变 FINISHED
- [x] 定时任务失败时有日志记录，不影响主流程

---

## 🏃 项目库

- [x] category = STANDARD 的项目删除时返回 403
- [x] 有赛事正在使用的项目删除时返回 409
- [x] 新建赛事项目时校验 itemStartTime 在赛事时间范围内
- [x] 有报名记录的赛事项目删除时返回 409，提示已有X人报名

---

## 🎽 运动员申请

- [x] 赛事状态不是 OPEN 时申请返回 409
- [x] 同一用户同一赛事重复申请返回 409
- [x] 申请成功后自动同步 realName/department/grade/phone 到 sys_user（方案C）
- [x] SUPER_ADMIN 和 EVENT_ADMIN 角色不可申请，返回 403
- [x] 审核通过后异步发送站内通知
- [x] 审核拒绝后异步发送站内通知（含拒绝原因）
- [x] 已审核的申请重复审核返回 409
- [x] 批量审核只处理 PENDING 状态，已审核的跳过并在结果中说明

---

## 📝 项目报名（7项校验逐一验证）

- [x] 校验1：账号 DISABLED 时返回「账号已被禁用」
- [x] 校验2：未通过运动员审核时返回「请先申请并通过运动员资格审核」
- [x] 校验3：不在报名时间内返回「不在报名时间范围内」
- [x] 校验4：名额已满返回「该项目报名人数已满」
- [x] 校验5：重复报名同一项目返回「您已报名该项目，请勿重复报名」
- [x] 校验6：超过项目数上限返回「已达到本赛事最多报名X个项目的限制」
- [x] 校验7：时间冲突返回「与您已报名的[项目名]时间冲突」
- [x] 名额扣减使用原子 UPDATE（current_count < max_participants 条件）
- [x] UPDATE 影响行数为 0 时返回 409（并发安全）

---

## ❌ 取消报名

- [x] 只能取消自己的报名，取消他人返回 403
- [x] 报名截止后取消返回 409
- [x] 取消成功后 current_count 原子减 1
- [x] 状态置为 CANCELLED，不物理删除

---

## 🎨 前端

- [x] 赛事列表页只显示非草稿赛事
- [x] 赛事详情页正确展示当前用户的运动员申请状态
- [x] 赛事详情页正确展示当前用户已报名的项目
- [x] 名额已满的项目报名按钮灰显并显示原因
- [x] 报名截止后取消按钮不可点击
- [x] 管理后台赛事列表包含草稿状态
- [x] 发布赛事前的校验错误提示清晰（缺项目/缺管理员）

---

## 代码质量

- [x] 无遗留 System.out.println / console.log
- [x] 所有涉及多表写操作的 Service 方法有 @Transactional
- [x] 定时任务方法有异常捕获，不因单次失败影响下次执行
