---
description: 统一提交切分与自动化门禁规范
alwaysApply: true
---

# 自动化提交与门禁规范

## 提交切分
- 一次提交只对应一个可验收目标（单一 concern）。
- 同一目标的代码、测试、文档状态回写应在同一提交内完成。
- `BACKLOG.md` 的任务状态变更必须与对应实现同批提交。

## 提交信息
- 使用 `type(scope): subject`，如 `test(backend): ...`、`fix(backend): ...`。
- `subject` 用动词短语描述“完成的目标”，避免仅写“update/fix issue”。

## 自动化门禁
- 后端改动提交前必须通过：`mvn -Pnon-container-baseline test`。
- 前端改动提交前必须通过：`npm run lint` 与 `npm test`。
- 若同时改动前后端，先执行前端门禁，再执行后端门禁。

## 失败处理
- 门禁失败先自动修复并重跑。
- 同一问题连续失败 2 次后，标记任务为 `BLOCKED` 并写明 `block_reason`。
