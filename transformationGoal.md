
---

# lz_sports 单校版体育赛事管理系统 PRD

**文档版本**：v1.0 ｜ **创建日期**：2026-03-03

---

## 一、项目目标

| 目标 | 说明 |
|------|------|
| 开箱即用 | 部署后通过初始化向导配置学校信息，立即可用 |
| 角色清晰 | 学校管理员 / 赛事管理员 / 运动员，权限边界分明 |
| 数据隔离 | 单校部署天然隔离；预留多校 SaaS 扩展点 |
| 品牌定制 | 支持学校 Logo、主题色、校名等个性化配置 |

---

## 二、用户故事

### 学校管理员

- **US-001**：作为学校管理员，我希望在系统初次启动时完成学校信息配置（校名、Logo），以便系统呈现本校品牌形象。
- **US-002**：作为学校管理员，我希望审核新用户的注册申请，以便确保只有本校师生可以使用系统。
- **US-003**：作为学校管理员，我希望创建赛事并指定赛事管理员，以便将具体事务委托给相关负责人。
- **US-004**：作为学校管理员，我希望查看全校赛事数据总览（参赛人数、赛事数量），以便掌握整体运营情况。
- **US-005**：作为学校管理员，我希望管理年级/院系等基础数据，以便运动员注册时正确选择所属组织。

### 赛事管理员

- **US-006**：作为赛事管理员，我希望查看和编辑被分配给我的赛事，以便维护赛事信息准确性。
- **US-007**：作为赛事管理员，我希望逐条或批量审核报名申请并填写拒绝原因，以便保证报名资格合规。
- **US-008**：作为赛事管理员，我希望手动或批量导入成绩，以便快速完成赛后数据录入。
- **US-009**：作为赛事管理员，我希望导出报名名单和成绩表（Excel），以便用于线下存档和公示。

### 运动员

- **US-010**：作为运动员，我希望用 QQ 邮箱注册并等待管理员审核激活，以便参与报名。
- **US-011**：作为运动员，我希望浏览已发布赛事并在线报名，以便参加比赛。
- **US-012**：作为运动员，我希望实时查看报名审核状态和个人成绩，以便了解参赛情况。

---

## 三、核心业务流程

### 3.1 系统初始化（首次部署）

```
首次启动 → 初始化向导
  Step 1：填写学校信息（校名、Logo、联系邮箱）
  Step 2：创建超级管理员账号
  Step 3：配置年级/院系基础数据
  Step 4：完成 → 进入系统首页
```

> 向导仅在数据库为空时展示，完成后不再出现。超级管理员不可删除。

---

### 3.2 用户注册与审核

```
运动员填写注册表单（邮箱/密码/姓名/年级/学号）
    ↓
系统发送邮箱验证码 → 用户完成验证
    ↓
账号进入「待审核」状态
    ↓
学校管理员审核
  通过 → 账号激活，用户收邮件通知
  拒绝 → 注明原因，用户收邮件，可重新申请
```

**关键规则**：
- 仅支持 `@qq.com` 邮箱注册，前后端双重校验。
- 同一邮箱不可重复注册。
- 超时 72 小时未审核，系统自动提醒管理员。

---

### 3.3 赛事创建与发布

```
学校管理员创建赛事
  填写：名称、描述、封面图、时间、地点
  配置：比赛项目（名称/性别限制/人数上限）
  设置：报名资格规则（年级限制，可选）
  指定：赛事管理员
    ↓
保存为「草稿」→ 审阅 → 发布
    ↓
「报名中」→ 截止后自动变「报名结束」→「已结束」归档
```

**赛事状态流转**：`草稿 → 报名中 → 报名结束 → 进行中 → 已结束`（报名开始前可撤回至草稿）

---

### 3.4 报名与资格校验

```
运动员提交报名
    ↓
系统自动校验（全部通过才可提交）：
  ✓ 账号已激活？
  ✓ 在报名时间内？
  ✓ 满足年级/性别要求？
  ✓ 项目未达人数上限？
  ✓ 未重复报名同一项目？
    ↓
进入「待审核」队列 → 赛事管理员审核
  通过 → 运动员收确认通知
  拒绝 → 注明原因，运动员可重新报名
```

---

### 3.5 成绩录入与公示

```
赛事管理员进入成绩管理
  方式一：手动逐条录入
  方式二：下载模板 → 填写 → 批量导入 Excel
    ↓
系统校验数据格式 → 保存为「草稿成绩」（仅管理员可见）
    ↓
确认无误 → 发布成绩 → 运动员可在个人中心查看
```

---

## 四、功能模块与优先级

### 学校管理员

| 模块 | 功能 | 优先级 |
|------|------|--------|
| 系统配置 | 学校信息、Logo、主题色、年级/院系管理 | P0 |
| 用户管理 | 注册审核、用户列表、禁用、角色分配 | P0 |
| 赛事管理 | 创建/编辑/发布/归档赛事、指定赛事管理员 | P0 |
| 数据统计 | 总览仪表盘、赛事维度统计 | P1 |
| 系统公告 | 发布全站公告 | P1 |

### 赛事管理员

| 模块 | 功能 | 优先级 |
|------|------|--------|
| 我的赛事 | 查看/编辑被分配赛事 | P0 |
| 报名审核 | 逐条/批量审核、拒绝原因 | P0 |
| 成绩管理 | 手动录入、Excel 导入、发布成绩 | P0 |
| 数据导出 | 名单/成绩表 Excel 导出 | P1 |

### 运动员端

| 模块 | 功能 | 优先级 |
|------|------|--------|
| 注册/登录 | QQ 邮箱注册、邮箱验证 | P0 |
| 赛事浏览 | 列表+详情、报名状态标签 | P0 |
| 在线报名 | 提交报名、查看审核状态 | P0 |
| 个人中心 | 报名记录、成绩查询、资料修改 | P0 |

---

## 五、界面原型建议（文字描述）

**初始化向导**：居中卡片 + 顶部步骤条（1/4），逐步引导。Step 3 年级默认预填「大一～大四/研究生」可增删。

**管理员控制台**：左侧菜单（仪表盘/用户管理/赛事管理/系统配置）+ 顶部 Logo + 通知铃铛。首页展示 4 个数据卡片（待审核用户、进行中赛事、本月参赛人次、注册总数）。

**赛事创建页**：左侧表单 + 右侧实时预览卡片，项目以「可展开标签卡」动态增删。底部固定操作栏：保存草稿 / 预览 / 发布。

**报名审核页**：顶部审核进度统计（待审核 N / 已通过 N / 已拒绝 N），主体表格支持多选批量操作，拒绝弹窗填写原因。

**运动员赛事列表**：卡片网格，含封面图、截止时间、参赛状态标签（未报名/审核中/已通过），移动端单列响应式布局。

**成绩录入页**：左侧项目切换列表，右侧可内联编辑的成绩表格，顶部「导入 Excel / 下载模板 / 发布成绩」操作区，导入后展示成功/失败详情。

---

## 六、技术方案建议

### 架构选型

```
Vue 3 + Element Plus（前端）
    ↕ REST API + JWT
Spring Boot（后端）
    ├─ AOP + 自定义注解 @RequireRole（权限拦截）
    ├─ MyBatis-Plus + 租户插件（数据访问）
    └─ Spring Mail + 异步队列（邮件通知）
MySQL 8.x + Redis + MinIO（文件存储）
```

### 权限控制（RBAC）

- 三个角色：`SCHOOL_ADMIN` / `EVENT_ADMIN` / `ATHLETE`
- 赛事管理员与赛事绑定存于 `event_admin_mapping(event_id, user_id)` 关联表
- 每次接口调用后端验证 `event_id` 与当前用户绑定关系，防止越权

### 多校扩展预留

所有核心业务表（用户、赛事、报名）加 `school_id` 字段，默认值为 1。未来升级 SaaS 时只需：① 开放学校注册入口，② MyBatis-Plus 开启租户插件按 `school_id` 过滤，无需重构表结构。

### 关键技术选型

| 需求 | 方案 |
|------|------|
| Excel 导入/导出 | EasyExcel |
| 文件存储 | MinIO（本地自部署，兼容 S3） |
| 邮件发送 | Spring Boot Mail + 异步任务 |
| 并发报名超卖防护 | 数据库乐观锁 / `SELECT FOR UPDATE` |

---

## 七、核心数据表（概要）

| 表名 | 核心字段 | 说明 |
|------|----------|------|
| `school_config` | name, logo_url, theme_color | 学校配置，仅一条记录 |
| `sys_user` | email, role, status, grade_id, school_id | 所有用户 |
| `grade` | name, school_id | 年级字典 |
| `event` | name, status, reg_deadline, school_id | 赛事主表 |
| `event_item` | event_id, name, gender_limit, max_count | 比赛项目 |
| `event_admin_mapping` | event_id, user_id | 赛事管理员绑定 |
| `registration` | user_id, event_id, item_id, status, reject_reason | 报名记录 |
| `result` | registration_id, score, rank, is_published | 成绩记录 |

---

## 八、验收标准

### 功能验收清单

**系统初始化**
- [ ] 首次启动展示向导，完成后不再出现
- [ ] Logo 上传后全站顶部正确展示校名和 Logo

**用户注册与审核**
- [ ] 非 QQ 邮箱前端实时报错，无法提交
- [ ] 审核通过/拒绝后用户均收到邮件通知
- [ ] 管理员在 30 秒内可找到并完成审核操作

**赛事管理**
- [ ] 草稿赛事运动员端不可见
- [ ] 报名截止时间到达后，报名入口自动关闭
- [ ] 赛事管理员只能操作被分配的赛事（访问其他返回 403）

**报名**
- [ ] 不符合资格时按钮灰显并显示具体原因
- [ ] 并发 50 人同时报名，不出现超额录入

**成绩**
- [ ] 未发布成绩时运动员看不到
- [ ] 发布后运动员个人中心可查看成绩和排名

### 非功能验收

| 指标 | 标准 |
|------|------|
| 页面响应 | 主要列表页首屏 < 2 秒（局域网） |
| 数据导出 | 百条以内 Excel 导出 < 5 秒 |
| 移动端 | 运动员端核心页面手机浏览器正常展示，无横向滚动 |
| 权限隔离 | 越权接口调用返回 403 |

---