task_id: BE-013
goal: Cover uploadLogo success path and exception mapping path in non-container tests.
scope_in_repo:
  - lz_sports_backend/src/test/java/com/lz/service/impl/SchoolConfigServiceImplTest.java
out_of_scope:
  - lz_sports_frontend/**
  - infra/**
  - migrations/**
validation_commands:
  - cd lz_sports_backend && mvn -Pnon-container-baseline test
rollback_notes: Revert newly added uploadLogo success/failure tests if they introduce instability.

execution_steps:
  - Add test for valid logo upload that returns URL and updates school config.
  - Add test for update failure that is wrapped as BusinessException(\"上传失败\").
  - Run baseline profile tests and record result under runs/.
