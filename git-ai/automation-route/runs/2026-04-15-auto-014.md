run_id: 2026-04-15-auto-014
related_task_id: AUTO-014
changed_files:
  - lz_sports_backend/src/main/java/com/lz/service/impl/RegistrationServiceImpl.java
  - lz_sports_backend/src/main/java/com/lz/mapper/RegistrationMapper.java
  - lz_sports_backend/src/test/java/com/lz/service/impl/RegistrationServiceImplTest.java
  - BACKLOG.md
  - git-ai/automation-route/runs/2026-04-15-auto-014.md
key_changes:
  - Fixed reapply gate to allow both CANCELLED and REJECTED records to be reactivated to PENDING.
  - Fixed active-registration SQL to count only PENDING/APPROVED/CONFIRMED for quota and conflict checks.
  - Added unit test `addShouldReactivateRejectedRegistrationInsteadOfInsert`.
  - Replayed admin approve/refuse and verified notification list + unread count changes.
test_results:
  - "mvn -Pnon-container-baseline test: passed (33 tests)"
  - "POST /api/registration/apply/1 -> code=200"
  - "POST /api/registration/apply/5 -> code=200"
  - "PUT /api/registration/attend/{id}?eventId=1 -> code=200"
  - "PUT /api/registration/refuse/{id}?eventId=1 -> code=200"
  - "GET /api/notification/unread-count: 6 -> 8"
  - "GET /api/notification/page: latest records include one 审核通过 and one 审核拒绝 notification"
risks:
  - Replay relies on local smoke fixture users and local DB state cleanup before each scenario.
  - Notification content encoding in terminal output is garbled under current shell codepage, though API codes and counts are correct.
issue_inputs:
  - source: business-replay
    signal: reapply on REJECTED record returned business 409 and blocked notification replay setup
root_causes:
  - Service duplicate guard allowed only CANCELLED reactivation.
  - Mapper active-count query treated REJECTED as active (`status <> 'CANCELLED'`), causing false quota exhaustion.
fixes_applied:
  - Allowed REJECTED in reapply reuse path in `RegistrationServiceImpl.add`.
  - Updated `RegistrationMapper` active-count and conflict SQL to explicit active statuses.
  - Added regression unit test for REJECTED reapply behavior.
token_source:
  - athleteToken: /api/auth/login response data.token (smoke_user_447613714)
  - adminToken: /api/auth/login response data.token (event_admin_smoke)
regression_watch:
  - Add integration test for notification unread-count increment after attend/refuse operations.
  - Keep replay fixture reset script to avoid manual cleanup drift.
repeated_failures:
  - issue_key: rejected-record-counted-as-active
    attempts: 2
    blocked: false
next_action: Prepare split commit for BE-018 backend fix and AUTO-014 run/backlog docs update.
