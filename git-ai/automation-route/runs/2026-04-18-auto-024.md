run_id: 2026-04-18-auto-024
related_task_id: AUTO-024
scope:
  - ensure frontend on 5173 and backend on 8080
  - run linkup smoke + notification smoke using existing accounts
  - validate role-based permission boundaries (SUPER_ADMIN/SCHOOL_ADMIN/EVENT_ADMIN/USER/ATHLETE)
changed_files:
  - BACKLOG.md
  - scripts/smoke-rolepaths.ps1
  - git-ai/automation-route/runs/2026-04-18-auto-024.md
key_changes:
  - Added `scripts/smoke-rolepaths.ps1` to replay multi-role API permission checks with existing fixture accounts
  - Recorded an end-to-end regression run (ports + full smoke + role checks) in automation-route runs
test_results:
  - service_ports:
      frontend: "http://localhost:5173 => 200 (Vite dev server)"
      backend: "GET /api/system/init-status => code=200,data=true"
  - smoke_linkup:
      cmd: "powershell -ExecutionPolicy Bypass -File scripts/smoke-linkup.ps1 -FrontendUrl http://localhost:5173 -BackendUrl http://localhost:8080 -Username ops_athlete_b -Password Pass12345"
      result: pass
  - smoke_notification:
      cmd: "powershell -ExecutionPolicy Bypass -File scripts/smoke-notification.ps1 -BackendUrl http://localhost:8080 -Username ops_athlete_b -Password Pass12345 -AdminUsername ops_event_admin_a -AdminPassword Pass12345"
      result: pass
  - role_paths:
      cmd: "powershell -ExecutionPolicy Bypass -File scripts/smoke-rolepaths.ps1 -BackendUrl http://localhost:8080"
      result: pass
risks:
  - event_admin_fixture_binding:
      note: "RequireEventAdmin endpoints may depend on event-admin binding for a specific eventId. Current run observed event-admin batch-audit returning 403 for eventId=1 (fixture mapping missing), but other privilege boundaries and role checks passed."
  - local_env_dependency:
      note: "Backend requires local DB/Redis + env vars (DB_PASSWORD/JWT_SECRET/etc). Missing env will fail startup or key endpoints."
issue_inputs:
  - source: local-run
    signal: "backend failed to start due to missing JWT_SECRET and Spring Security default user empty, and later init-status returned 500 due to DB auth"
root_causes:
  - Missing required env placeholders in dev profile (JWT/MAIL/OSS) caused bean creation failure
  - Local MySQL root password required (access denied when DB_PASSWORD not provided)
  - Port 8080 was occupied by a previous Java process after a failed start attempt
fixes_applied:
  - Started backend with explicit local env vars (DB/Redis/JWT/security placeholders) and cleared port 8080 listener before restart
  - Added role-path smoke script that uses existing fixture accounts for repeatable API permission checks
token_source:
  - "All tokens obtained via POST /api/auth/login response data.token during this run (no manual copy)"
regression_watch:
  - "Add a stable event-admin fixture mapping for a known eventId so RequireEventAdmin positive paths can be asserted deterministically"
repeated_failures:
  - issue_key: backend-missing-env-placeholders
    attempts: 1
    blocked: false
  - issue_key: backend-db-access-denied
    attempts: 1
    blocked: false
next_action: "Append a TODO to make event-admin fixture binding deterministic (e.g. seed event-admin mapping for eventId=1) and extend role smoke to assert RequireEventAdmin positive path."

