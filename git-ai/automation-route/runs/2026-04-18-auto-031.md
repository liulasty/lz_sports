run_id: 2026-04-18-auto-031
related_task_id: AUTO-030
changed_files:
  - git-ai/automation-route/runs/2026-04-18-auto-031.md
key_changes:
  - Executed deeper event lifecycle replay beyond oneclick on localhost env (event selection, apply, approve probe, score publish, public ranking, event status transition).
  - Re-ran with deterministic EVENT_ADMIN binding on eventId=1 to eliminate permission noise.
test_results:
  - overall: blocked
  - attempts: 2
  - attempt_1:
      event_id: 2
      apply: "code=409 (athlete profile prerequisite missing)"
      publish_score: "code=403 (event-admin not bound to selected event)"
      public_ranking: "code=500 (SQL error: unknown column g.name)"
      change_status_closed: "code=403 (event-admin not bound to selected event)"
  - attempt_2:
      event_id: 1
      bind_event_admin: "code=200"
      apply: "code=409 (athlete profile prerequisite missing)"
      publish_score: "code=500 (SQL error: unknown column remark in result table)"
      public_ranking: "code=500 (SQL error: unknown column g.name in joined table)"
      change_status_closed: "code=200"
      change_status_ongoing: "code=200"
risks:
  - "Score publish/public ranking currently blocked by DB schema drift vs mapper SQL, preventing full lifecycle acceptance."
  - "Athlete apply path depends on complete athlete profile fixture; without profile data, registration apply returns 409."
issue_inputs:
  - source: local-run
    signal: "score publish/public ranking returned SQLSyntaxErrorException (unknown columns remark / g.name)"
root_causes:
  - "Schema mismatch between backend score query SQL and local DB tables (result/department columns inconsistent)."
  - "Fixture data prerequisite missing for athlete apply (profile completeness required)."
fixes_applied:
  - "Bound EVENT_ADMIN to eventId=1 via SUPER_ADMIN before second replay."
repeated_failures:
  - issue_key: event-lifecycle-score-sql
    attempts: 2
    blocked: true
    block_reason: "Local DB schema incompatible with score publish/public ranking SQL (unknown columns), cannot complete full event lifecycle until schema/mapper alignment is fixed."
next_action: "Fix mapper SQL or align local schema for result/department columns, then rerun lifecycle replay from apply -> approve -> score publish -> public ranking."
