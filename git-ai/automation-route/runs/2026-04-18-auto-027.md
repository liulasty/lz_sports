run_id: 2026-04-18-auto-027
related_task_id: AUTO-027
changed_files:
  - BACKLOG.md
  - scripts/dev-start.ps1
  - scripts/test.bat
  - scripts/test.sh
  - git-ai/automation-route/runs/2026-04-18-auto-027.md
key_changes:
  - Added `scripts/dev-start.ps1` to load local env from `config/.env.dev`, adapt docker-oriented values to local runtime, cleanup occupied ports, and start frontend/backend with one command
  - Added `dev-start` subcommand to `scripts/test.bat` and `scripts/test.sh`
test_results:
  - cmd: "powershell -ExecutionPolicy Bypass -File scripts/dev-start.ps1 -DryRun"
    result: pass
    notes:
      - branch guard works
      - env file parse and local adaptation works
      - port cleanup/start steps planned output correct in dry-run
risks:
  - real start mode launches new PowerShell windows via Start-Process; this is intended for local interactive workflow
issue_inputs:
  - source: local-run
    signal: "previous startup blocked by missing env placeholders and occupied 8080/5173"
root_causes:
  - backend startup depends on a set of env vars not auto-loaded in shell
  - local dev used docker hostnames (`mysql`/`redis`) requiring localhost adaptation for direct host run
fixes_applied:
  - auto env load + variable mapping (`DB_USER` -> `DB_USERNAME`)
  - host mapping (`mysql`/`redis` -> `localhost`) for local run
  - automatic pre-start port cleanup for 8080/5173
token_source:
  - none
regression_watch:
  - if `.env.dev` structure changes, keep parser and mapping rules aligned
repeated_failures:
  - issue_key: local-startup-env-and-port
    attempts: 1
    blocked: false
next_action: "Wire dev-start usage into README local startup section for first-time onboarding."

