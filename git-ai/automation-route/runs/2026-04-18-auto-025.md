run_id: 2026-04-18-auto-025
related_task_id: AUTO-025
scope:
  - make RequireEventAdmin positive path deterministic by binding EVENT_ADMIN to eventId
  - rerun role-path permission smoke and full-smoke
changed_files:
  - BACKLOG.md
  - scripts/smoke-rolepaths.ps1
  - git-ai/automation-route/runs/2026-04-18-auto-025.md
key_changes:
  - `smoke-rolepaths.ps1` now binds EVENT_ADMIN to the target eventId via `/api/admin/events/{eventId}/admins` before asserting `batch-audit` success
  - Forced JSON array payload for single-element arrays in POST helper to avoid backend List<Long> deserialization errors
test_results:
  - role_paths:
      cmd: "powershell -ExecutionPolicy Bypass -File scripts/smoke-rolepaths.ps1 -BackendUrl http://localhost:8080 -EventId 1"
      result: pass
      highlights:
        - "Bind EVENT_ADMIN to eventId=1 => code=200"
        - "EVENT_ADMIN batch-audit (no-op ids) => code=200"
  - full_smoke:
      cmd: "powershell -ExecutionPolicy Bypass -File scripts/smoke-full.ps1 -BackendUrl http://localhost:8080 -FrontendUrl http://localhost:5173 -Username ops_athlete_b -Password Pass12345"
      result: pass
risks:
  - "EventId=1 is assumed to exist locally; if local DB differs, pass `-EventId` explicitly or seed the event baseline."
issue_inputs:
  - source: local-run
    signal: "AdminEventController expects JSON array; PowerShell single-element array JSON became scalar and caused 500"
root_causes:
  - "PowerShell ConvertTo-Json serializes single-element arrays as scalars, causing backend to reject payload for List<Long>"
fixes_applied:
  - "Forced single-element array payload to JSON array syntax in `Api-Post` helper"
  - "Bound EVENT_ADMIN to eventId via SUPER_ADMIN before RequireEventAdmin assertions"
token_source:
  - "Tokens obtained via POST /api/auth/login response data.token during this run"
regression_watch:
  - "Consider extending role smoke to also assert SUPER_ADMIN/SCHOOL_ADMIN can query event-admin list `/api/admin/events/{eventId}/admins`."
repeated_failures:
  - issue_key: admin-event-admins-json-array-singleton
    attempts: 1
    blocked: false
next_action: "Next iteration: add a single script entrypoint that checks branch+ports and runs full-smoke + role-path smoke in one command."

