run_id: 2026-04-15-auto-010
related_task_id: AUTO-010
changed_files:
  - BACKLOG.md
  - git-ai/automation-route/runs/2026-04-15-auto-010.md
key_changes:
  - Replayed athlete login and queried event/project/registration endpoints to continue business flow.
  - Confirmed registration apply is blocked and traced failure to event-project data mismatch.
  - Added BE-016 to backlog as the executable fix task for broken local linkage.
test_results:
  - "POST /api/auth/login -> code=200"
  - "GET /api/project/page -> total=12, first record eventId=1"
  - "GET /api/event/page -> total=1, only event id=2"
  - "GET /api/event/1 -> code=404"
  - "POST /api/registration/apply/1 -> code=409"
risks:
  - Current local sample data cannot support end-to-end apply/cancel replay until event-project linkage is repaired.
issue_inputs:
  - source: business-replay
    signal: registration apply repeatedly returns business 409 while project list is visible
  - source: api-cross-check
    signal: project references eventId=1 but event query only returns id=2
root_causes:
  - Visible project records are linked to a non-existent event row in current local data snapshot.
fixes_applied:
  - No direct data write applied in this run to avoid unreviewed local DB mutation.
  - Converted diagnosis into actionable backlog task BE-016 with acceptance criteria.
token_source:
  - accessToken: /api/auth/login response data.token
regression_watch:
  - After BE-016, re-run apply/cancel replay with the same athlete account and record registration id lifecycle.
repeated_failures:
  - issue_key: registration-apply-missing-event-link
    attempts: 1
    blocked: false
next_action: Execute BE-016 by repairing event-project linkage in local seed/runtime data and replay apply->cancel flow.
