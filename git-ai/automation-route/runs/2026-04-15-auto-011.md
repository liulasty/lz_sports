run_id: 2026-04-15-auto-011
related_task_id: AUTO-011
changed_files:
  - lz_sports_backend/src/main/java/com/lz/service/impl/RegistrationServiceImpl.java
  - lz_sports_backend/src/main/java/com/lz/common/aspect/PermissionAspect.java
  - lz_sports_backend/src/test/java/com/lz/service/impl/RegistrationServiceImplTest.java
  - BACKLOG.md
  - git-ai/automation-route/runs/2026-04-15-auto-011.md
key_changes:
  - Fixed registration reapply logic: when existing record is CANCELLED, reactivate it to PENDING instead of inserting a new row.
  - Fixed event-admin permission parsing: `RequireEventAdmin` now prefers request `eventId` and named `eventId` argument before generic Long fallback.
  - Added unit test `addShouldReactivateCancelledRegistrationInsteadOfInsert` to lock the reapply behavior.
  - Replayed event-admin approve/refuse flow successfully after fixes.
test_results:
  - "mvn -Pnon-container-baseline test: passed (32 tests)"
  - "POST /api/registration/apply/1 -> code=200"
  - "PUT /api/registration/attend/{id}?eventId=1 -> code=200"
  - "POST /api/registration/apply/3 -> code=200"
  - "PUT /api/registration/refuse/{id}?eventId=1 -> code=200"
  - "GET /api/registration/page confirms statuses: item1=APPROVED, item3=REJECTED"
risks:
  - Replay still depends on local DB bootstrap data and local event-admin fixture user (`event_admin_smoke`).
  - Local DB patches (user/event/mapping fixture data) are not tracked by git and need scripted setup for repeatability.
issue_inputs:
  - source: api-replay
    signal: apply after cancel failed with Duplicate entry (uk_user_item)
  - source: permission-check
    signal: event-admin审核请求在报名ID与赛事ID不同场景下返回403
root_causes:
  - Service layer expected reapply after cancel, but persistence path always used insert, conflicting with unique index.
  - Permission aspect resolved first Long argument as eventId, causing registrationId to be used for event-admin mapping lookup.
fixes_applied:
  - Reused existing CANCELLED registration row via `updateById` path in `RegistrationServiceImpl.add`.
  - Added deterministic eventId resolution order in `PermissionAspect`.
  - Added focused unit test for cancelled-to-pending reactivation.
token_source:
  - athleteToken: /api/auth/login(data.token) for smoke_user_447613714
  - adminToken: /api/auth/login(data.token) for event_admin_smoke
regression_watch:
  - Add integration test covering event-admin approve/refuse with non-equal registrationId/eventId values.
  - Consider fixture script for local replay accounts and event mappings to reduce manual DB setup.
repeated_failures:
  - issue_key: registration-reapply-duplicate-key
    attempts: 2
    blocked: false
  - issue_key: require-event-admin-wrong-id-resolution
    attempts: 1
    blocked: false
next_action: Continue full closure with registration batch-audit replay and negative permission tests for non-admin users.
