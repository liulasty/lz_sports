# lz_sports 单校版改造升级步骤文档

**目标**：打造开箱即用、无需二次开发的单校体育赛事管理系统（Web PC端）。
**当前状态**：完成 Spring Boot 3 + Vue 3 技术栈迁移，具备基础 CRUD 功能。
**待办重点**：系统初始化向导、注册审核流程、赛事全流程管理、成绩/Excel 模块。

---

## 第一阶段：系统初始化与多校底座 (System Init & Foundation)
> **目标**：实现“部署即用”，通过向导完成学校信息配置。

### 1.1 数据库结构调整
- [ ] **新增 `school_config` 表**：存储校名、Logo、主题色、邮箱配置。
- [ ] **新增 `grade` 表**：存储年级/院系字典。
- [ ] **预留 `school_id`**：核心表 (user, event, project, registration) 预留字段 (暂默认 1)。

### 1.2 初始化向导 API (Backend)
- [ ] 创建 `SystemInitController`。
- [ ] `GET /check-init`：检查是否已存在超级管理员/学校配置。
- [ ] `POST /init`：接收学校信息 + 超管账号 + 年级数据，一次性初始化。
- [ ] 拦截器调整：未初始化时，所有非静态资源请求重定向至初始化页。

### 1.3 初始化向导页面 (Frontend)
- [ ] 新增 `views/init/index.vue`。
- [ ] 步骤条组件 (Steps)：
    1. 学校信息配置 (校名/Logo)。
    2. 超级管理员注册。
    3. 基础数据预设 (年级/院系)。
- [ ] 路由守卫：访问系统前先调 `/check-init`，未初始化强制跳转。

---

## 第二阶段：用户注册与权限体系 (User & Auth)
> **目标**：确保只有本校师生可注册，且权限隔离明确。

### 2.1 邮箱验证与注册流程
- [ ] **后端**：
    - 集成 `Spring Mail`，实现发送 QQ 邮箱验证码接口。
    - `register` 接口增加验证码校验逻辑。
    - 注册后状态默认为 `PENDING` (待审核)。
- [ ] **前端**：
    - 注册页增加“发送验证码”按钮 (倒计时 60s)。
    - 注册成功提示“等待管理员审核”。

### 2.2 审核与角色管理
- [ ] **后端**：
    - `UserController` 新增审核接口 `approve(id, status)`。
    - 完善 `UserRole`：区分 `SCHOOL_ADMIN` (校管), `EVENT_ADMIN` (赛管), `ATHLETE` (运动员)。
- [ ] **前端**：
    - 校管后台新增「用户审核」页面：列表展示待审核用户，支持通过/拒绝。
    - 拒绝时需填写原因 (弹窗)。

---

## 第三阶段：赛事全流程增强 (Event Lifecycle)
> **目标**：实现赛事从草稿到归档的完整闭环，以及权限下放。

### 3.1 赛事状态流转
- [ ] **后端**：
    - `Event` 实体增加 `status` (DRAFT, PUBLISHED, ENDED)。
    - 新增接口：发布赛事、撤回赛事、结束赛事。
    - 只有 `PUBLISHED` 状态的赛事对运动员可见。
- [ ] **前端**：
    - 赛事管理页增加状态标签。
    - 增加“发布”、“结束”操作按钮。

### 3.2 赛事管理员分配
- [ ] **后端**：
    - 新增 `event_admin_mapping` 表 (或在 Event 表加 `admin_id`)。
    - 权限拦截：`EventController` 操作需校验当前用户是否为该赛事的管理员。
- [ ] **前端**：
    - 创建赛事时，可选择/指定负责的管理员账号。
    - 赛事管理员登录后，只能看到自己负责的赛事。

### 3.3 严格报名校验
- [ ] **后端**：
    - 校验规则增强：性别符合? 年级符合? 人数未满? 时间未截止?
    - 解决并发超卖问题 (数据库乐观锁)。

---

## 第四阶段：成绩管理与数据交互 (Score & Data)
> **目标**：高效的数据录入与导出，满足线下存档需求。

### 4.1 Excel 导入导出
- [ ] **后端**：
    - 引入 `EasyExcel` 依赖。
    - 实现 `RegistrationController` 导出报名名单接口。
    - 实现 `ScoreController` 导入成绩接口。
- [ ] **前端**：
    - 报名管理页增加“导出名单”按钮。
    - 成绩管理页增加“导入成绩”按钮。

### 4.2 成绩录入与公示
- [ ] **后端**：
    - 新增 `Score` 相关 API：录入、修改、发布。
    - 成绩发布前，运动员不可见。
- [ ] **前端**：
    - 成绩录入表格 (支持行内编辑)。
    - 运动员个人中心查看成绩。

---

## 第五阶段：UI/UX 优化与部署 (Polish & Deploy)
- [ ] **UI 定制**：读取 `school_config` 动态替换系统 Logo 和主题色。
- [ ] **仪表盘**：完善管理员首页统计图表 (ECharts)。
- [ ] **部署包**：提供 Docker Compose 一键部署脚本 (包含 MySQL, Redis, MinIO, Backend, Frontend)。

